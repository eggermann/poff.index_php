never add php.index eg pages/dominikeggermann.com/index.php, these ar th buildet stuff
(src/includes/scripts.built.php) and src/includes/header.built.php do ignore
never read the builded index.php as context
edit only src/, build/, scripts/, tests/ unless user says otherwise
never edit anything in pages/ directly (generated output)
never edit src/includes/header.built.php and src/includes/scripts.built.php (generated by asset build)
do not run runtime entrypoints for checks (php -r include ..., MCP routes) unless user asks; they can rewrite poff.config.json and poff.config.toon
do not commit changes to poff.config.json or poff.config.toon unless explicitly requested
keep <!-- NAV_PLACEHOLDER --> in src/includes/layout.php (used by build injection)
run npm run test:mcp and php -l on changed PHP files after edits
run npm run build only when user explicitly asks for rebuild
